{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/", "dest": "/signup" },
    { "src": "/signup", "dest": "/signup" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
