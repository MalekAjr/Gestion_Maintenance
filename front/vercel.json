{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/app/(.*)", "dest": "/app/index.html" },
    { "src": "/app/(.*)", "dest": "/app/signup" }
  ]
}
